import{_ as e,c as a,o,V as n}from"./chunks/framework.DVGDG1kt.js";const f=JSON.parse('{"title":"Modo Red","description":"","frontmatter":{},"headers":[],"relativePath":"guides-and-concepts/network-mode.md","filePath":"guides-and-concepts/network-mode.md"}'),r={name:"guides-and-concepts/network-mode.md"},d=n('<h1 id="modo-red" tabindex="-1">Modo Red <a class="header-anchor" href="#modo-red" aria-label="Permalink to &quot;Modo Red&quot;">​</a></h1><div class="info custom-block"><p class="custom-block-title">INFO</p><p>La fuente original (en ingles) de este tutorial se encuentra <a href="https://tanstack.com/query/latest/docs/framework/vue/guides/network-mode" target="_blank" rel="noreferrer">aquí</a></p></div><p>TanStack Query proporciona tres modos de red diferentes para distinguir cómo deben comportarse las <a href="./queries.html">Consultas</a> y las <a href="./mutations.html">Mutaciones</a> si no tiene conexión de red. Este modo se puede configurar para cada Consulta / Mutación individualmente o globalmente a través de los valores predeterminados de consulta / mutación.</p><p>Dado que TanStack Query se utiliza con mayor frecuencia para la recuperación de datos en combinación con bibliotecas de recuperación de datos, el modo de red predeterminado es <a href="./network-mode.html#modo-de-red-en-linea">en línea</a>.</p><h2 id="modo-de-red-online" tabindex="-1">Modo de Red: online <a class="header-anchor" href="#modo-de-red-online" aria-label="Permalink to &quot;Modo de Red: online&quot;">​</a></h2><p>En este modo, las consultas y mutaciones no se activarán a menos que tenga una conexión de red. Este es el modo por defecto. Si se inicia una recuperación para una consulta, siempre permanecerá en el <code>state</code> (<code>pending</code>, <code>error</code>, <code>success</code>) en el que se encuentra si la recuperación no se puede realizar porque no hay conexión de red. Sin embargo, también se expone un <a href="./queries.html#fetchstatus">fetchStatus</a>. Esto puede ser:</p><ul><li><code>fetching</code>: El <code>queryFn</code> realmente se está ejecutando: una solicitud está en curso.</li><li><code>paused</code>: La consulta no se está ejecutando; está <code>paused</code> hasta que vuelva a tener conexión</li><li><code>idle</code>: La consulta no está recuperando ni pausada</li></ul><p>Los indicadores <code>isFetching</code> e <code>isPaused</code> se derivan de este estado y se exponen por conveniencia.</p><blockquote><p><em>&quot;Tenga en cuenta que puede que no sea suficiente comprobar el estado <code>pending</code> para mostrar un control giratorio de carga. Las consultas pueden estar en <code>state: &#39;pending</code>, pero <code>fetchStatus: &#39;paused&#39;</code> si se están montando por primera vez y no tiene conexión de red.&quot;</em></p></blockquote><p>Si se ejecuta una consulta porque está en línea, pero se desconecta mientras aún se realiza la recuperación, TanStack Query también pausará el mecanismo de reintento. Las consultas pausadas continuarán ejecutándose una vez que recupere la conexión de red. Esto es independiente de <code>refetchOnReconnect</code> (que también es <code>true</code> por defecto en este modo), porque no es un <code>refetch</code>, sino un <code>continue</code>. Si mientras tanto la consulta ha sido <a href="./query-cancellation.html">cancelada</a>, no continuará.</p><h2 id="modo-de-red-always" tabindex="-1">Modo de Red: always <a class="header-anchor" href="#modo-de-red-always" aria-label="Permalink to &quot;Modo de Red: always&quot;">​</a></h2><p>En este modo, TanStack Query siempre buscará e ignorará el estado online / offline. Es probable que este sea el modo que desee elegir si utiliza TanStack Query en un entorno donde no necesita una conexión de red activa para que sus consultas funcionen; si acaba de leer desde <code>AsyncStorage</code>, o si simplemente desea devolver <code>Promise.resolve(5)</code> desde su <code>queryFn</code>.</p><ul><li>Las consultas nunca serán <code>paused</code> porque no tengas conexión de red.</li><li>Los reintentos tampoco se detendrán: su consulta pasará al estado <code>error</code> si falla.</li><li><code>refetchOnReconnect</code> el valor predeterminado es <code>false</code> en este modo, porque volver a conectarse a la red ya no es un buen indicador de que las consultas obsoletas deben recuperarse. Aún puedes activarlo si lo deseas.</li></ul><h2 id="modo-de-red-offlinefirst" tabindex="-1">Modo de Red: offlineFirst <a class="header-anchor" href="#modo-de-red-offlinefirst" aria-label="Permalink to &quot;Modo de Red: offlineFirst&quot;">​</a></h2><p>Este modo es el punto medio entre las dos primeras opciones, donde TanStack Query ejecutará <code>queryFn</code> una vez, pero luego pausará los reintentos. Esto es muy útil si tiene un <em>serviceWorker</em> que intercepta una solicitud de almacenamiento en caché como en una <a href="https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Tutorials/js13kGames/Offline_Service_workers" target="_blank" rel="noreferrer">PWA primero fuera de línea</a>, o si usa el almacenamiento en caché HTTP a través del <a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Caching#the_cache-control_header" target="_blank" rel="noreferrer">encabezado de Control de Caché.</a>.</p><p>En esas situaciones, la primera recuperación puede tener éxito porque proviene de un <em>offline storage / cache</em>. Sin embargo, si hay una pérdida de caché, la solicitud de red se interrumpirá y fallará, en cuyo caso este modo se comporta como una consulta <code>online</code>: pausa los reintentos.</p><h2 id="herramientas-de-desarrollo" tabindex="-1">Herramientas de Desarrollo <a class="header-anchor" href="#herramientas-de-desarrollo" aria-label="Permalink to &quot;Herramientas de Desarrollo&quot;">​</a></h2><p><a href="./../get-start/devtools.html">TanStack Query Devtools</a> mostrará las consultas en un estado <code>paused</code> si se están recuperando, pero no hay conexión de red. También hay un botón de alternancia para <em>Simular el comportamiento fuera de línea</em>. Tenga en cuenta que este botón en realidad <em>no</em> afectará su conexión de red (puede hacerlo en las herramientas de desarrollo del navegador), pero configurará <a href="https://tanstack.com/query/latest/docs/reference/onlineManager" target="_blank" rel="noreferrer">OnlineManager</a> en un estado fuera de línea.</p><h2 id="signatura" tabindex="-1">Signatura <a class="header-anchor" href="#signatura" aria-label="Permalink to &quot;Signatura&quot;">​</a></h2><ul><li><code>networkMode: &#39;online&#39; | &#39;always&#39; | &#39;offlineFirst&#39;</code><ul><li>opcional</li><li>por defecto a <code>&#39;online&#39;</code></li></ul></li></ul>',20),t=[d];function s(c,i,l,u,p,m){return o(),a("div",null,t)}const b=e(r,[["render",s]]);export{f as __pageData,b as default};
